AI Use:

Task 1: 

Perplexity and Claude was used to debug errors in the initial setup of OpenClaw, as well as with the VirtualBox VM. 

Task 2:
 
What does mkdir -p ~/csce465-agentsec/hw1/{bin,web,markers,evidence} do?

Can you write me a sh file using these restrictions?

Write bin/safe_marker.sh. It must:
• accept exactly one argument, course-marker;
• reject all other arguments;
• create only hw1/markers/marker.txt;
• never run another program supplied by user input.
